apiVersion: v1
kind: ConfigMap

metadata:
  name: my-api-config
  namespace: development

data:
  DATABASE_HOST: postgresql.development.svc.cluster.local
  DATABASE_PORT: "5432"
  DATABASE_NAME: appdb